{
  "name": "tardis-net-server-app",
  "version": "0.1.0",
  "private": true,
  "description": "Tailwind build for the tardis-net console. The generated CSS is committed, so Node is only needed when the styles change.",
  "scripts": {
    "build": "tailwindcss -i app/static/css/tailwind.css -o app/static/css/app.css --minify",
    "watch": "tailwindcss -i app/static/css/tailwind.css -o app/static/css/app.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.11",
    "tailwindcss": "^4.1.11"
  }
}
